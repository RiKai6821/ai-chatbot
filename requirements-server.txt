# Server/container deps: core + dashscope for the /voice endpoint (STT/TTS).
# (pyaudio/pyttsx3 are only for the PC client voice.py; the server doesn't need them.)
# Keep this file ASCII so `pip install -r` works on Windows (GBK locale) too.
-r requirements.txt
dashscope>=1.20.0
