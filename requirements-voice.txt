# Stage 3 voice deps (install after requirements.txt):
#   .\venv\Scripts\python.exe -m pip install -r requirements-voice.txt
# ASCII-only so `pip install -r` works on Windows (GBK locale) too.
SpeechRecognition>=3.10.0
pyttsx3>=2.90
pyaudio>=0.2.14
dashscope>=1.20.0
